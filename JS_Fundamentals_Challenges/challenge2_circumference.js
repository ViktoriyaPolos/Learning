// PI * radius * radius

//note: for constants we usually use capital letters


let radius = 10;
const PI = Math.PI;
const area = PI*radius*radius;
const circumference = 2*PI*radius;
console.log(area + ' m2');
console.log(circumference + ' m');

const area2 = PI*radius**2;
console.log(area2 + ' m2');